using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestDataManager : MonoBehaviour
{
    
    // Update is called once per frame
    void Update()
    {/*
        if (Input.GetKeyDown(KeyCode.O))
        {
            List<MonsterSpawnInfo> dataTest = await DataLoadManager.Instance.LoadJsonAsync<List<MonsterSpawnInfo>>("StageWaveData");

            DataLoadManager.Instance.RegisterTable<MonsterSpawnInfo>(dataTest);

            var data = DataLoadManager.Instance.GetData<MonsterSpawnInfo>(100002);

            if (data != null)
            {
                Debug.Log($"ID:{data.ID}, {data.checkPrevWaveIndexes.Count}");
            }
            var data2 = DataLoadManager.Instance.GetData<MonsterSpawnInfo>(100001);

            if (data2 != null)
            {
                Debug.Log($"ID:{data2.ID}, {data2.checkPrevWaveIndexes.Count}");
            }
        }
        */
        
    }
}
